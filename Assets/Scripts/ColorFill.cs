using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColorFill : MonoBehaviour
{
    [SerializeField] PhysicMaterial newColor, targetColor;
    int x, y;

    //void ColorFill(GameObject[][] grids, int x, int y)
    //{
    //    if(gris)

    //    ColorFill(grids, x + 1, y);
    //    ColorFill(grids, x - 1, y);
    //    ColorFill(grids, x, y + 1);
    //    ColorFill(grids, x, y - 1);
    //}
}
